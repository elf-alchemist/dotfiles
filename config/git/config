[user]
	name  = Guilherme M. Miranda
	email = alchemist.software@proton.me
[init]
	defaultBranch = main
[core]
	editor   = vim
	pager    = less
	eol      = lf
	autocrlf = input
	safecrlf = true
	fileMode = true
[color]
	ui = auto
[status]
	relativePaths      = false
	branch             = true
	aheadBehind        = true
	showStash          = true
	showUntrackedFiles = normal
[log]
	date           = iso8601
	decorate       = short
	showSignatures = true
[diff]
	tool    = vimdiff
	guitool = vimdiff
[push]
	default         = current
	autoSetupRemote = true
[pull]
	ff     = true
	rebase = false
[merge]
	log           = false
	ff            = true
	tool          = vimdiff
	guitool       = vimdiff
	conflictstyle = diff3
[help]
	format      = man
	browser     = man
	autoCorrect = prompt
[alias]
	ls = !git config --list --global             | sed '/^alias/d'

	st = status --short --branch --ahead-behind --untracked-files=all
	as = add    .
	aa = add    --all
	ca = commit --amend
	ce = commit --amend --no-edit
	co = commit --message
	cm = commit --message "changes"
	dw = diff   --color-words='[^[:space:]]|([[:alnum:]]|UTF_8_GUARD)+'

	qwik = !git add --all && git st

	cb      = checkout
	cb-main = checkout main
	cb-prev = checkout -
	cb-new  = checkout -b
	cb-all  = branch --list --all
	cb-url  = branch --list --remotes
	cb-del  = branch --delete

	url     = remote
	url-all = remote --verbose
	url-get = remote get-url --all origin
	url-set = remote set-url origin
	url-add = remote set-url --add

	un       = reset HEAD~1
	un-soft  = reset HEAD~1 --soft
	un-hard  = reset HEAD~1 --hard
	un-stage = restore --staged

	log-all  = log --oneline --graph
	log-stat = log --oneline --graph --stat
	log-prev = log --stat --decorate=full --full-diff --max-count=1 HEAD~1

	toptal = "!_toptal() { curl -sL https://www.toptal.com/developers/gitignore/api/$@ ;}; _toptal"
