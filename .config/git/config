[user]
	name = Guilherme M. Miranda
	email = alchemist.software@proton.me
	useConfigOnly = true
[init]
	defaultBranch = main
[core]
	editor = vim
	pager = less
	eol = lf
	autocrlf = input
	safecrlf = true
	fileMode = true
[color]
	ui = auto
[status]
	relativePaths = false
	branch = true
	aheadBehind = true
	showStash = true
	showUntrackedFiles = normal
[log]
	date = iso8601
	decorate = full
	showSignatures = true
[diff]
	tool = nvimdiff
	guitool = nvimdiff
[push]
	default = current
	autoSetupRemote = true
[pull]
	ff = true
	rebase = false
	twohead = manual
	octopus = manual
[merge]
	log = false
	ff = true
	tool = nvimdiff
	guitool = nvimdiff
	conflictstyle = merge
[help]
	format = man
	browser = man
	autoCorrect = prompt
[alias]
	# List Config: General, Alias, Where, Enhanced
	ls-global = !git config --list --global  | sed '/^alias/d'
	ls-where  = !git config --list --show-origin
	ls-alias  = !git config --global --get-regexp alias | sed 's/alias\\.//g'
	ls-better = !git ls-alias | awk '{printf \"| %-12s | %s\\n\", $1, $0}' | sed '1,3d'

	# Basic Commands
	st = !git status --untracked-files=all --short --show-stash
	as = !git add    --all && git status
	co = !git commit --message
	ca = !git commit --amend

	# Checkout, branching
	ch      = !git checkout
	ch-main = !git checkout main
	ch-prev = !git checkout -
	ch-new  = !git checkout -b
	br-all  = !git branch --list --all
	br-del  = !git branch --delete

	# Remote management
	url-all = !git remote --verbose
	url-get = !git remote get-url --all origin
	url-set = !git remote set-url origin
	url-add = !git remote set-url --add

	# Undo
	undo      = !git reset HEAD~1
	undo-soft = !git reset HEAD~1 --soft
	undo-hard = !git reset HEAD~1 --hard

	# Updating
	fetch2date = !git stash --include-untracked && git fetch && git status
	pull2date  = !git stash --include-untracked && git pull  && git status

	# Logging
	log-prev   = !git log --stat -1 HEAD
	log-all    = !git log --all --oneline --graph
	log-head   = !git log --stat HEAD~1..origin/HEAD
	log-main   = !git log --stat main~1..origin/main
	log-trunk  = !git log --stat trunk~1..origin/trunk
	log-master = !git log --stat master~1..origin/master

	# Cloning GitHub
	hub      = !git clone https://github.com/$1
	hub-self = !git clone https://github.com/lambda-alchemist/$1
	hub-conf = !git clone https://github.com/lambda-alchemist/.dotfiles.git

	# Cloning GitLab
	lab      = !git clone https://gitlab.com/$1
	lab-self = !git clone https://gitlab.com/alkemista1/$1
	lab-conf = !git clone https://gitlab.com/alkemista1/dotfiles.git

	# Generate .gitignore
	toptal = "!_toptal() { curl -sL https://www.toptal.com/developers/gitignore/api/$@ ;}; _toptal"
